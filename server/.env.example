# Local / Railway template — NÃO commitar senhas reais
# No Railway: Variables do serviço (não use arquivo .env em produção)

# Supabase Postgres (Connect → Transaction pooler, porta 6543)
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# CORS do frontend em produção
FRONTEND_URL=https://stockpyrou.com.br

# PORT e HOST: Railway define PORT; HOST padrão é 0.0.0.0
# PORT=3001
# HOST=0.0.0.0
